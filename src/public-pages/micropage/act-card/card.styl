.card-box
  width 640rpx
  height 480rpx

.container {
  position: relative;
  transition: 1s;
  transform-style: preserve-3d;
}

.card-container
  position: relative;
  transition: .5s;
  transform-style: preserve-3d;
  //scale: .9
  //transform: scale(.9);

.show-front {
  transform: rotateY(0deg);
}

.show-back {
  transform: rotateY(180deg);
}

.card-front {
  transform: rotateY(0deg);
  backface-visibility: hidden;
}

.card-back {
  backface-visibility: hidden;
  transform: rotateY(180deg);
}

.animate
  animation: animate .5s linear infinite;

@keyframes animate {
  to {
    //transform: rotate3d(1,1,1,360deg);
    transform: rotateY(360deg);
  }
}

.ani-style {
  animation: aniStyle 1s ease-in-out 1;
  transform translate(0, 0)
}

@keyframes aniStyle {
  0% {

  }
  50% {
    top 50%;
    left 50%
    transform translate(-50%, -50%)
  }

  100% {

  }
}

.text-scroll-x-1
  transform: translateX(0);
  animation: 10s wordsLoop1 linear infinite normal;

.text-scroll-x-2
  animation: 10s wordsLoop2 linear infinite normal;
  transform: translateX(100%);

@keyframes wordsLoop1 {
  0% {
    transform: translateX(0);
  }
  100% {
    transform: translateX(-100%);
  }
}

@keyframes wordsLoop2 {
  0% {
    transform: translateX(100%);
  }
  100% {
    transform: translateX(0);
  }
}

.bg-light {
  //width: 547rpx;
  //height: 848rpx;
  //background: linear-gradient(180deg, rgba(255, 214, 0, 0) 0%, #FFD600 50%,rgba(255, 214, 0, 0) 100%);
  //opacity: 0.6;
  //filter: blur(50rpx);
  //border-radius 50%

  background: url("https://media-opens.oss-cn-shanghai.aliyuncs.com/2076/default/image/b5462a8f7ac4b067a65e41c6b89139aa.png") no-repeat center center / 100% 100%
}

.prize-dlg-title {
  width: 560rpx;
  //height: 120rpx;
  background: linear-gradient(90deg, rgba(255, 255, 255, 0) 0%, rgba(255, 255, 255, .4) 52%, rgba(255, 255, 255, 0) 100%);
  border: 2rpx solid;
  border-image: linear-gradient(90deg, rgba(255, 255, 255, 0), rgba(255, 255, 255, 1), rgba(255, 255, 255, 0)) 2 2;
  font-size: 32rpx;
  font-family: PingFangSC-Semibold, PingFang SC;
  font-weight: 600;
  color: #FDFFCF;
  line-height: 44rpx;
  text-align center
  padding 12rpx 56px
}

.prize-img-animate
  animation: 1s prize-image-animate linear 1;

@keyframes prize-image-animate {
  0% {
    transform: scale(.2);
  }
  50% {
    transform: scale(1.3);
  }
  100% {
    transform: scale(.8);
  }

}
.card-mask
  position relative
.card-mask:after
  content ""
  position absolute
  top 0
  bottom 0
  left 0
  right 0
  background rgba(0,0,0,.3)